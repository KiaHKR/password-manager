#include <../include/FileManager.hpp>

using namespace std;

class FileManager
{
private:
    
public:
    FileManager(/* args */);
    ~FileManager();
};

FileManager::FileManager(/* args */)
{
    auto file = make_shared<FILE>;
}
